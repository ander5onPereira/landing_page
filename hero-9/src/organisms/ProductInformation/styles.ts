import styled from "styled-components";

export const Container = styled.div`
  width: 50%;
  h1 {
    color: var(--darkblue);
    font-size: 3.5rem;
    font-weight: bold;
  }
  h6 {
    font-size: 1.1rem;
    color: rgba(30, 37, 94, 0.7);
    font-weight: normal;
    padding-right: 10rem;
    margin-top: 1.6rem;
  }
`;
